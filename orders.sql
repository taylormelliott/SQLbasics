-- CREATE TABLE orders(
--   order_id SERIAL PRIMARY KEY,
--   person_id INT,
--   product_name VARCHAR(20),
--   product_price INT,
--   quantity INT
--   );
  
--   INSERT INTO orders (person_id, product_name, product_price, quantity)
--   VALUES (1, 'Air pods', 200, 1);
  
-- select * from orders;

--   INSERT INTO orders (person_id, product_name, product_price, quantity)
--   VALUES (2, 'MacPro', 2000, 1);

--  INSERT INTO orders (person_id, product_name, product_price, quantity)
--   VALUES (2, 'Snacks', 50000, 2);

--  INSERT INTO orders (person_id, product_name, product_price, quantity)
--   VALUES (1, 'Gold', 2, 100);

--  INSERT INTO orders (person_id, product_name, product_price, quantity)
--   VALUES (1, 'energy drink', 5, 5);

-- select * from orders;

-- select SUM(quantity) from orders;

-- select SUM(product_price) from orders;

-- select SUM(product_price) from orders
-- where person_id = 1;




  

