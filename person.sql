-- CREATE TABLE person(
-- 	person_id SERIAL PRIMARY KEY,
--   first_name VARCHAR(20),
--   last_name VARCHAR(20),
-- 	age INT,
--   height_cm INT,
--   city VARCHAR(20),
--   favorite_color VARCHAR(20)
-- );

-- INSERT INTO person (first_name, last_name, age, height_cm, city, favorite_color)
-- VALUES ('Libby', 'Leonard', 28, 152.4, 'Layton', 'pink')

-- select * from person;

-- INSERT INTO person (first_name, last_name, age, height_cm, city, favorite_color)
-- VALUES ('Taylor', 'Elliott', 30, 182.88, 'Kaysville', 'purple')

-- INSERT INTO person (first_name, last_name, age, height_cm, city, favorite_color)
-- VALUES ('Michelle', 'Barret', 33, 167.64, 'Farmington', 'purple')

-- INSERT INTO person (first_name, last_name, age, height_cm, city, favorite_color)
-- VALUES ('Rachel', 'Burnett', 27, 180.33, 'Smyrna', 'blue')

-- select * from person;

-- INSERT INTO person (first_name, last_name, age, height_cm, city, favorite_color)
-- VALUES ('Megan', 'Heaps', 34, 167.64, 'Logan', 'green')

-- select * from person;

-- select * from person order by height_cm Desc;

-- select * from person order by height_cm;

-- select * from person order by age Desc;

-- select * from person
-- where age > 20;

-- select * from person;

-- DELETE from person
-- where person_id = 5;

-- select * from person;

-- select * from person
-- where age = 18;

-- select * from person
-- where age < 20 OR age > 30;

-- select * from person

-- select * from person
-- where age != 27;

-- select * from person
-- where favorite_color != 'red';

-- select * from person
-- where favorite_color != 'red' AND favorite_color != 'blue';

-- select * from person
-- where favorite_color = 'orange' OR favorite_color = 'green';

-- select * from person
-- where favorite_color IN ('orange', 'green', 'blue');

-- select * from person
-- where favorite_color IN('green','orange');

-- select * from person
-- where favorite_color IN ('orange', 'green', 'blue');

-- select * from person
-- where favorite_color IN ('yellow', 'purple');












